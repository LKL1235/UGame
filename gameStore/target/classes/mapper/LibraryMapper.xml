<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.myApp.gamestore.mapper.LibraryMapper">

    <select id="getLibrary" resultType="com.myApp.gamestore.entity.Library">
        SELECT * FROM ${name}
    </select>
</mapper>
